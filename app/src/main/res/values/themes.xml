<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AutochangedPhoneBackgrounds" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="colorOnSurface">@color/surface_light</item>
        <item name="colorOnSecondary">@color/nav</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorOnBackground">#FBFBFB</item>
        <item name="colorOnSecondaryContainer">#fff</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.AutochangedPhoneBackgrounds.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.AppCompat.Light.NoActionBar.FullScreen" parent="@style/Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>


    <style name="Theme.AutochangedPhoneBackgrounds.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.AutochangedPhoneBackgrounds.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>